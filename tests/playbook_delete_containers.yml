---

- hosts: all
  gather_facts: false
  tasks:
    - name: Remove Docker containers
      docker_container:
        name: "{{ inventory_hostname }}"
        state: absent
      delegate_to: localhost
...
