---
- name: Report when JumpCloud API cleanup is skipped
  ansible.builtin.debug:
    msg: >-
      JumpCloud API cleanup is skipped because no API key is configured. Local
      test containers will still be removed when enabled.
  when: not jumpcloud_test_run_api_cleanup | bool
  no_log: true
